# Storage Configuration for PlayCanvas Workspaces and Builds

# Default filesystem disk (local, s3, etc.)
FILESYSTEM_DISK=local

# Workspace source files storage
# Options: local, s3, or any configured disk
WORKSPACE_STORAGE_DISK=local

# Build artifacts storage
# Options: local, s3, or any configured disk
BUILDS_STORAGE_DISK=s3

# Build retention policy (days)
BUILD_RETENTION_DAYS=30

# Temporary directory for build processes
WORKSPACE_TEMP_DIR=/tmp

# AWS S3 Configuration (if using S3 for storage)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=your-bucket-name
AWS_URL=https://your-bucket.s3.amazonaws.com

# Example configurations:

# Configuration 1: Everything local
# WORKSPACE_STORAGE_DISK=local
# BUILDS_STORAGE_DISK=local

# Configuration 2: Workspaces local, builds on S3
# WORKSPACE_STORAGE_DISK=local
# BUILDS_STORAGE_DISK=s3

# Configuration 3: Everything on S3
# WORKSPACE_STORAGE_DISK=s3
# BUILDS_STORAGE_DISK=s3